  import { useToast } from  '../../utils/toastProvider'

  
  const { showToast } = useToast()

jata bata data featch gara ko xa tetaii bta 


const data = await fetchMenuByRestaurant(restData.restaurantId,showToast);
